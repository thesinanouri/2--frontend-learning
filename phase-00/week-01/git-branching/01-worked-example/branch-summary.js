const currentBranchTopic = "Creating and merging a branch";
const branchName = "practice/update-branch-summary";

console.log(`Current topic: ${currentBranchTopic}`);
console.log(`Branch: ${branchName}`);
