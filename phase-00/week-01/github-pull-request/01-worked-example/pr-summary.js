const baseBranch = "main";
const compareBranch = "practice/github-pr-workflow";
const proposedChange = "Add GitHub pull request practice";

console.log(`Base branch: ${baseBranch}`);
console.log(`Compare branch: ${compareBranch}`);
console.log(`Proposed change: ${proposedChange}`);
